function togglePopup() {
    const overlay = document.getElementById('popupOverlay');
    overlay.classList.toggle('show');
}

function toggleCusPopup() {
    const overlay = document.getElementById('popupCusOverlay');
    overlay.classList.toggle('show');
}